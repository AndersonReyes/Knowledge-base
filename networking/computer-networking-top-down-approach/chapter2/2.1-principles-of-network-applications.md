# 2.1 Principles of Network Applications

> Kurose, J. & Ross, K. (2022). *Computer Networking: A Top-Down Approach* (8th ed.), Section 2.1.

---

## Core Premise

Network application development means writing programs that run on **end systems** and communicate over the network. You never write software for network-core devices (routers, switches) — those operate at the network layer and below. Confining application software to end systems is what enables rapid application development and deployment.

```mermaid
flowchart LR
    subgraph HostA["Host A"]
        A5[Application] --> A4[Transport]
        A4 --> A3[Network]
        A3 --> A2[Link]
        A2 --> A1[Physical]
    end
    subgraph Core["Network Core (routers)"]
        R3[Network] --> R2[Link]
        R2 --> R1[Physical]
    end
    subgraph HostB["Host B"]
        B5[Application] --> B4[Transport]
        B4 --> B3[Network]
        B3 --> B2[Link]
        B2 --> B1[Physical]
    end
    A1 -.->|"logical app-layer channel"| B5
    A1 --> R1
    R1 --> B1
```

---

## 2.1.1 Network Application Architectures

### Client-Server

- **Always-on server** with a fixed, well-known IP address
- Clients do not communicate directly with each other
- A single server host often can't handle load → **data centers** used to create a powerful virtual server
  - Data centers can have **hundreds of thousands of servers** (must be powered and maintained)
  - Service providers pay recurring **interconnection and bandwidth costs** for sending data from data centers
  - Google has **19 data centers** distributed around the world (handling Search, YouTube, Gmail, etc.)
- Examples: Web (HTTP), FTP, Telnet, e-mail, Google/Bing/Baidu, Amazon/eBay/Alibaba, Gmail/Yahoo Mail, Facebook/Instagram/Twitter/WeChat

### Peer-to-Peer (P2P)

- Minimal or no reliance on dedicated servers in data centers
- Peers communicate directly without passing through a dedicated server
- Peers are **not owned by the service provider** — they are desktops and laptops controlled by users, residing in homes, universities, and offices
- **Self-scalability**: each peer that requests files also adds service capacity by distributing files to others
- **Cost effective**: no significant server infrastructure or bandwidth costs (contrast with client-server + datacenters)
- Challenges: security, performance, reliability due to highly decentralized structure
- Example: BitTorrent

```mermaid
flowchart TD
    subgraph CS["(a) Client-Server"]
        Server["Always-on Server\n(fixed IP)"]
        C1[Client 1] --> Server
        C2[Client 2] --> Server
        C3[Client 3] --> Server
    end
    subgraph P2P["(b) Peer-to-Peer"]
        P1[Peer 1] <--> P2[Peer 2]
        P2 <--> P3[Peer 3]
        P3 <--> P1
        P1 <--> P4[Peer 4]
        P4 <--> P2
    end
```

---

## 2.1.2 Processes Communicating

### Processes vs. Programs

Communication happens between **processes** (running program instances), not programs. Processes on different hosts communicate by exchanging **messages** across the network. Processes communicating with each other reside in the **application layer** of the five-layer protocol stack.

### Client and Server Processes

| Role | Definition |
|------|------------|
| Client process | Initiates communication at the beginning of the session |
| Server process | Waits to be contacted to begin the session |

This labeling applies per communication session. In P2P, a single peer can be both client and server across different sessions (or simultaneously in different sessions).

### Sockets

A **socket** is the interface between the application layer and the transport layer — also referred to as the **Application Programming Interface (API)** between the application and the network, since the socket is the programming interface with which network applications are built.

Analogy: a process is like a house; its socket is like the door. When a process sends a message to another process on another host, it shoves the message out its door (socket), assuming there is a transportation infrastructure on the other side to deliver it. Once the message arrives at the destination host, it passes through the receiving process's door (socket).

```
Application process
       |
    [socket]   ← API boundary; developer controls above this line
       |
Transport layer (TCP/UDP)
       |
Network layer
```

- Sending: process pushes message out through its socket
- Receiving: message arrives at destination socket, process reads it
- Developer controls: (1) choice of transport protocol, (2) a few transport-layer parameters (max buffer size, max segment size)

The application developer controls everything on the application side of the socket, but has little control on the transport side (choice of protocol, and some buffer/segment size parameters).

```mermaid
flowchart LR
    subgraph HostA["Host A"]
        PA["Process A"] <-->|"send/recv"| SA["Socket"]
        SA <--> TA["Transport\nLayer"]
    end
    subgraph HostB["Host B"]
        PB["Process B"] <-->|"send/recv"| SB["Socket"]
        SB <--> TB["Transport\nLayer"]
    end
    TA <-->|"Internet"| TB
```

### Addressing Processes

To deliver a message to a specific process, two pieces of information are required:

1. **IP address** — 32-bit quantity uniquely identifying the host
2. **Port number** — identifies the specific receiving process (socket) within the host

Well-known port assignments (from [iana.org](https://www.iana.org)):
- HTTP: port 80
- SMTP: port 25

---

## 2.1.3 Transport Services Available to Applications

Transport-layer services can be classified along four dimensions:

### 1. Reliable Data Transfer

- Packets can be lost in transit (buffer overflow, bit corruption, etc.)
- **Reliable data transfer**: guarantee that all data is delivered correctly and completely
- Applications that cannot tolerate loss: e-mail, file transfer, web documents, financial apps
- **Loss-tolerant applications**: multimedia (audio/video) — a lost packet results in a small glitch, not a failure

### 2. Throughput

- Available throughput fluctuates as sessions share bandwidth and come and go
- **Bandwidth-sensitive applications**: require a minimum guaranteed throughput (e.g., Internet telephony encodes voice at **32 kbps** — receiving half the needed throughput is of little or no use)
- **Elastic applications**: can use whatever throughput is available; more is always better (e-mail, file transfer, web)
- Some multimedia applications use **adaptive coding** to adjust to available throughput

### 3. Timing

- Example guarantee: every bit pumped into the socket arrives at receiver within 100 ms
- Required by interactive real-time applications: VoIP, virtual environments, teleconferencing, multiplayer games
- Non-real-time apps prefer lower delay but have no hard constraint

### 4. Security

- Transport layer can encrypt data at the sender and decrypt at receiver → **confidentiality**
- Additional services: data integrity, end-point authentication (covered in Chapter 8)

---

## 2.1.4 Transport Services Provided by the Internet

The Internet provides two transport protocols: **TCP** and **UDP**.

### Application Requirements Reference Table

| Application | Data Loss | Throughput | Time-Sensitive |
|---|---|---|---|
| File transfer/download | No loss | Elastic | No |
| E-mail | No loss | Elastic | No |
| Web documents | No loss | Elastic (few kbps) | No |
| Internet telephony / Video conferencing | Loss-tolerant | Audio: few kbps–1 Mbps; Video: 10 kbps–5 Mbps | Yes: 100s of ms |
| Streaming stored audio/video | Loss-tolerant | Same as above | Yes: few seconds |
| Interactive games | Loss-tolerant | Few kbps–10 kbps | Yes: 100s of ms |
| Smartphone messaging | No loss | Elastic | Yes and no |

### TCP Services

**Connection-oriented service**
- Client and server exchange transport-layer control information (**handshaking**) before application-level messages begin to flow
- Handshaking alerts the client and server, allowing them to prepare for an onslaught of packets
- After handshaking, a TCP connection exists between the sockets of the two processes
- Connection is **full-duplex**: both processes can send messages to each other at the same time
- Connection must be **torn down** when finished

**Reliable data transfer**
- Delivers all bytes sent, in order, without errors or duplicates
- Application passes a **stream of bytes** into the socket; TCP guarantees the same byte stream arrives at the receiving socket with no missing or duplicate bytes

**Congestion control**
- Throttles the sending process when the network between sender and receiver is congested
- Limits each TCP connection to its fair share of network bandwidth
- For the general welfare of the Internet, not the direct benefit of the communicating processes

**What TCP does NOT provide**: throughput guarantees, timing guarantees

TCP's congestion control mechanism throttles a sender when the network is congested — this benefits the general welfare of the Internet rather than the individual application. Neither TCP nor UDP provides throughput or timing guarantees.

**Security enhancement**: TCP can be enhanced at the **application layer** with **TLS (Transport Layer Security)** to provide security services (encryption, data integrity, end-point authentication). TLS is not a third Internet transport protocol — it is an enhancement implemented in the application layer.

### UDP Services

UDP is a **no-frills, lightweight** transport protocol providing minimal services.

- **Connectionless**: no handshaking before the two processes start to communicate
- **Unreliable data transfer**: no guarantee that messages reach the receiving process
- **No ordering guarantee**: messages that do arrive may arrive out of order
- **No congestion control**: the sending side of UDP can pump data into the network layer at any rate it pleases
  - Note: actual end-to-end throughput may still be less than the pumped rate due to limited transmission capacity of intervening links or congestion

**What UDP does NOT provide**: reliable delivery, ordering, congestion control, throughput guarantees, timing guarantees

### Services Not Provided by Either Protocol

Neither TCP nor UDP provides **throughput guarantees** or **timing guarantees**. Time-sensitive applications (VoIP, streaming) work because they are designed to cope with this lack — but clever design has limits under excessive delay or insufficient throughput. Today's Internet can often provide satisfactory service to time-sensitive applications, but cannot guarantee timing or throughput.

### Why Applications Use UDP Despite Its Limitations

- Internet telephony (e.g., Skype, SIP/RTP) often prefers UDP because it can **tolerate some loss** but requires a **minimal rate** to be effective — TCP's congestion control would throttle the sender
- Many firewalls block UDP traffic → telephony apps often fall back to TCP as a backup

### Popular Applications: Protocol Summary (Figure 2.5)

E-mail, remote terminal access, Web, and file transfer all use TCP — primarily because TCP provides reliable data transfer, guaranteeing all data eventually reaches its destination.

| Application | Application-Layer Protocol | Underlying Transport Protocol |
|---|---|---|
| Electronic mail | SMTP [RFC 5321] | TCP |
| Remote terminal access | Telnet [RFC 854] | TCP |
| Web | HTTP 1.1 [RFC 7230] | TCP |
| File transfer | FTP [RFC 959] | TCP |
| Streaming multimedia | HTTP (e.g., YouTube), DASH | TCP |
| Internet telephony | SIP [RFC 3261], RTP [RFC 3550], or proprietary (e.g., Skype) | UDP or TCP |

---

## 2.1.5 Application-Layer Protocols

An **application-layer protocol** defines the rules for how processes on different end systems pass messages to each other. It specifies:

- **Message types**: e.g., request messages and response messages
- **Syntax**: fields in each message type and how fields are delineated
- **Semantics**: meaning of the information in each field
- **Rules**: when and how a process sends messages and responds to messages

### Public vs. Proprietary Protocols

- **Public (RFC-defined)**: HTTP [RFC 7230], SMTP [RFC 5321], FTP [RFC 959], etc. — any conforming implementation can interoperate
- **Proprietary**: e.g., Skype — intentionally not in the public domain

### Application ≠ Application-Layer Protocol

The application-layer protocol is one component of a network application, not the whole thing.

**Web application** components:
- HTML (document format standard)
- Web browsers (Chrome, IE)
- Web servers (Apache, Microsoft IIS)
- HTTP (the application-layer protocol — defines format and sequence of messages exchanged)

**Netflix** components (Section 2.6):
- Video storage and transmission servers
- Billing/client management servers
- Client apps (smartphone, tablet, computer)
- DASH (application-level protocol — defines format and sequence of messages between Netflix server and client)

> Note: An application-layer protocol is only **one piece** of a network application, albeit a very important piece from a networking perspective.

---

## 2.1.6 Network Applications Covered in Chapter 2

Five applications covered (in order):

1. **Web** (HTTP) — pervasive; protocol is straightforward and easy to understand
2. **Electronic mail** — the Internet's **first killer application**; more complex than the Web because it uses not one but **several** application-layer protocols
3. **DNS** — directory service for the Internet; most users invoke it indirectly through other applications (Web, file transfer, e-mail); illustrates how core network functionality (name-to-address translation) can be implemented at the application layer
4. **P2P file sharing**
5. **Video streaming on demand** — including distributing stored video over **content distribution networks (CDNs)**
